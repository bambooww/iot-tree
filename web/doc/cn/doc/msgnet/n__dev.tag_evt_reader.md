节点：标签事件/报警读取
==


此节点在参数配置时，会列举出项目中所有配置了事件/报警信息的标签。需要选择需要读取的相关标签即可。

当节点有任意消息进入时，会读取配置的标签，查看是否有事件或报警触发，最后形成一个JSON Array输出。

可以设置当没有任何触发时，不输出消息。此节点可以用来对一个项目的全部实际/报警进行处理，最简单的可以用来判断项目是否有触发事件或报警，中整体上做个警示等功能。


