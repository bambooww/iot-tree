/**
 * iottree server can run as a node in network.
 * 
 * based on project,you can share a project (named share A) as a node to other iottree project (name caller B),in which it will
 * make this share in it's one channel.
 * 
 * this package provider all support for sharer and caller
 * 
 * @author jason.zhu
 *
 */
package org.iottree.core.node;